{
  "name": "bookmark-nav-sync-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "migrate:local": "wrangler d1 migrations apply bookmark_nav_sync --local",
    "migrate:remote": "wrangler d1 migrations apply bookmark_nav_sync --remote"
  },
  "devDependencies": {
    "typescript": "^5",
    "wrangler": "^4.0.0"
  }
}
